{
  "name": "lighting",
  "version": "1.0.0",
  "private": true,
  "description": "A game built with Three.js, Howler.js, and Tween.js.",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --serve --watch",
    "deploy": "node scripts/build.mjs --deploy"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
